// Framer Motion requires plain hex/rgb strings (not CSS variables or Tailwind classes).
// This file centralises the values so they stay in sync with tailwind.config.js tokens.
export const THEME_COLORS = {
  gold: '#C9963A',
  mahogany: '#4A1C0E',
  warmBrown: '#6B3A2A',
} as const
